export enum FilterOperator {
  EQ = 'EQ',
  NEQ = 'NEQ',
  GT = 'GT',
  GTE = 'GTE',
  LT = 'LT',
  LTE = 'LTE',
  IN = 'IN',
  NOT_IN = 'NOT_IN',
  LIKE = 'LIKE',
  NOT_LIKE = 'NOT_LIKE',
  CONTAINS = 'CONTAINS',
  STARTS_WITH = 'STARTS_WITH',
  ENDS_WITH = 'ENDS_WITH',
}

export enum LogicalOperator {
  AND = 'AND',
  OR = 'OR',
}
export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}
